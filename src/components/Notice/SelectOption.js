import styled from 'styled-components';
import Colors from '../../constants/colors';

const SSelectOptionS = styled.select`
    float: right;
	font-weight: normal;
    width: 11.6rem;
	border-radius: 0px;
    padding: 0.4rem;
	color: ${Colors.select1};
	border-color: ${Colors.select2};
`;

export default SSelectOptionS;